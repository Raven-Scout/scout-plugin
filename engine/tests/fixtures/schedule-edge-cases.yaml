schema_version: 1

slots:
  bad-slot:
    type: not-real-type
    runner: run-scout.sh
    fires_at_local: "08:00"
    weekdays: [Mon]
    missed_window_hours: 4
    on_miss: fire
    cooldown_minutes: 60
